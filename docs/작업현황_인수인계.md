# 작업 현황과 다음 작업 지시 (2026-09-10 기준)

> 새 세션에서 이어서 작업할 사람(사람이든 Claude든)을 위한 문서. **이 문서 → `docs/기술적_계획.md` → `docs/날짜_해석_규칙.md` 순서로 읽으면 된다.**
> 제출 마감 2026-09-15(화) 23:59. 채점: 4코어 CPU, 인터넷 차단, 500장, 셀 타임아웃 2,400초(초과 시 CSV 미생성 → 정확도까지 0점), Python 3.10.

---

## 1. 지금 성능

| 측정셋 | 정확도 | 비고 |
| --- | --- | --- |
| **1차 500장 (블록 1~5, 측정 전용)** | **71.2% (356/500)** | 블록별 68~74%. 보고서에 적을 공식 수치 |
| 나머지 803장 (블록 6·7·9·10·14) | 73.0% (586/803) | 튜닝·분석용. 블록별 61~76%. 흐림(b) 36%, 작음(t) 33%, 날짜 2개+ 51% 가 약점 |
| 133장 (블록 2·7, 개발 중 기준) | 67.7% | 아래 이력 참고 |

속도: 14코어 PC 장당 1.68초. **4코어 시뮬레이션 장당 1.80초 = 500장 900초** (한도의 40%).

### 이력 (133장 기준, 하루 반 동안)

| 단계 | 정확도 | 4코어 장당 |
| --- | --- | --- |
| EasyOCR CRAFT + english_g2 (원래 파이프라인) | 39.1% | 약 5초 |
| + 후처리 규칙 5개 수정 | 44.4% | |
| 인식기 → PaddleOCR PP-OCRv5 rec | 60.9% | 5.81초 (한도 초과) |
| 탐지기 → PaddleOCR PP-OCRv5 det + 배치 인식 | **67.7%** | **1.80초** |

---

## 2. 현재 파이프라인 (`predict.ipynb`)

```
EXIF 보정 → 640px 축소 (후보 없으면 1024px 재시도)
 → PP-OCRv5 mobile det 탐지 (5MB, oneDNN 끔)
 → PP-OCRv5 mobile rec(en) 인식, 글자 크기순 8개씩 배치, 연도 포함 날짜 찾으면 작은 박스 건너뜀
 → 2패스: 후보 줄만 원본(≤2000px)에서 크롭해 재인식
 → 후처리: 오독 복원(2O27→2027, 0ct→Oct) → 정규식 4단계 → 연도 2017~2031 검증 → 가장 늦은 날짜
 → YYYY-MM-DD / NONE-MM-DD / YYYY-MM-NONE / NONE
```

- 후처리 규칙은 `docs/날짜_해석_규칙.md` 가 정본. 라벨링 도구 `notebooks/label.py` 와 파서가 같은 규칙. **운영진이 "모호한 값은 팀 후처리 규칙으로 채점"이라 확정했으므로 요약서 2p 에 이 규칙 표를 반드시 넣는다.**
- 환경변수: `ITDA_DET=craft`, `ITDA_REC=easyocr` 로 예전 EasyOCR 구성과 비교. `ITDA_THREADS=4` 로 스레드 제한. `ITDA_DEBUG=1` 로 장별 OCR 텍스트·후보 출력.
- 가중치는 `weights/` 에서만 오프라인 로드. `bash download_weights.sh` 로 받는다 (EasyOCR 2개 + Paddle 탐지·인식 폴더 2개).

### 검증된 것

- 오프라인: 모델 캐시 숨김 + 모든 HTTP 를 죽은 프록시로 보낸 상태에서 Run All 완주, 결과 동일.
- 4코어: `psutil.Process().cpu_affinity([0,1,2,3])` 로 nbconvert 를 띄우고 `ITDA_THREADS=4`. 133장 1.80초/장.
- 의존성: `requirements.txt` 를 `pip install --dry-run --python-version 3.10` 으로 win_amd64·manylinux 양쪽 해결 확인.

### 아직 안 한 검증 (Phase 5, 제출 전 필수)

- **진짜 Python 3.10 venv 를 새로 만들어 requirements 로 설치하고 Run All.** 이 PC 의 `.venv` 는 3.13 이다.
- 실제 네트워크 차단(방화벽) 상태 테스트. 지금까지는 프록시 시뮬레이션.

---

## 3. 파일 안내

| 파일 | 역할 |
| --- | --- |
| `predict.ipynb` | 제출 노트북. 첫 셀 CONFIG 형태 유지 |
| `notebooks/eval.py` | 예측 CSV 와 라벨 대조. `python notebooks/eval.py --pred submission.csv --blocks 1-5 --out eval.csv`. `--eda` 만 주면 라벨 분포 |
| `notebooks/exp_rec.py` | 인식기 비교 실험 (노트북 셀을 그대로 불러와 인식기만 교체). 새 인식 모델 시험할 때 |
| `notebooks/label.py` | 라벨링 도구. `normalize()` 가 날짜 규칙 정본 |
| `download_weights.sh` | 가중치 사전 다운로드 |
| `labels/labels_block*.csv` | 검증 라벨. 1차 = 블록 1~5 (측정 전용, 학습 금지), 2차 = 6~10, 3차 = 11~15 |
| `docs/기술적_계획.md` | 전체 계획과 실측 이력 |
| `docs/날짜_해석_규칙.md` | 후처리 규칙 정본 (요약서 삽입용) |
| `docs/QA_확정사항.md` | 운영진 Q&A |

### 측정 절차 (재현)

```bash
# 1) 라벨된 이미지만 폴더로 모으기 (예: 블록 1~5)
python - <<'EOF'
import pandas as pd, shutil, os
os.makedirs("val500", exist_ok=True)
for b in range(1, 6):
    for f in pd.read_csv(f"labels/labels_block{b}.csv", dtype=str)["file"]:
        shutil.copy(os.path.join("images", f), "val500")
EOF
# 2) 노트북 실행
ITDA_INPUT_DIR=./val500 ITDA_OUTPUT_PATH=./pred500.csv jupyter nbconvert --to notebook --execute predict.ipynb --output /tmp/run.ipynb
# 3) 평가
python notebooks/eval.py --pred pred500.csv --blocks 1-5 --out eval500.csv
```

---

## 4. 오답 분석 (1차 500장, 오답 144)

| 유형 | 건수 | 해상도 small/mid/large | 날짜 2개+ 태그 |
| --- | --- | --- | --- |
| 미검출 (NONE 출력) | 77 | 39 / 10 / 28 | 7 |
| 날짜 전부 틀림 | 37 | 18 / 8 / 11 | 12 |
| 월·일 틀림 | 23 | 10 / 6 / 7 | 2 |
| 연도만 틀림 | 7 | 2 / 1 / 4 | 0 |

읽는 법
- **미검출 77 이 절반.** 저해상도(39)에서 가장 많다. 640px 축소본에서 날짜를 못 잡는 것. 형식은 국산 YYYY.MM.DD 가 41건으로 특별한 형식 문제가 아니라 그냥 탐지·인식 실패.
- **전부 틀림 37 중 12 는 날짜가 2개 이상인 이미지.** 제조일자를 읽고 소비기한을 놓쳤거나 반대. 37 중 13 은 예측 연도가 정답 ±1 이라 "진짜 날짜를 오독"한 경우. 나머지는 엉뚱한 숫자열 오탐 가능성.
- 형식별로는 수입 DD.MM.YYYY 54.5%, DD.MM.YY 45.5% 가 약하다. 국산 76.3%.

---

## 5. 다음 작업 (우선순위 순)

### A. 해상도 사다리 실험 — 미검출 줄이기 (반나절, 무손실 가능성 높음)

시간 여유가 한도의 60% 남아 있다. `PASS1_LADDER = (640, 1024)` 를 바꿔 본다.
1. `(800, 1280)` 과 `(640, 1024, 1600)` 두 가지를 133장 또는 500장에서 정확도·시간 측정.
2. 저해상도(small) 원본은 확대해도 정보가 없으니 효과가 없을 수 있다 → 결과를 해상도별로 본다.
3. **4코어 시뮬레이션으로 시간 재확인.** 장당 4초를 넘기면 안 됨.
4. 탐지 파라미터도 후보: `TextDetection(..., limit_side_len=..., thresh=..., box_thresh=...)`. 기본 box_thresh 0.6 을 0.4 로 낮추면 흐린 글자 박스가 더 잡힌다.

### B. 오답 열어 보기 (2시간)

`ITDA_DEBUG=1` 로 오답 이미지만 돌려 OCR 이 실제로 뭘 읽었는지 본다. 특히 "전부 틀림" 25장(단일 날짜)이 오탐인지 오독인지. 오탐이 많으면 **키워드 앵커**(소비기한·유통기한·까지·EXP·BEST BEFORE 근처 줄에 가중치)를 후처리에 넣는 게 싸다. 이미 영문자를 읽고 있으니 한글은 못 읽지만 영문 키워드는 가능.

### C. 날짜 2개 이상 처리 (51장 중 21장 오답)

"가장 늦은 날짜" 규칙 자체는 맞다. 문제는 둘 중 하나를 못 읽어 나머지를 답하는 것. A·B 가 해결하면 같이 풀린다.

### D. Phase 3 인식기 파인튜닝 (2~3일, 가장 큰 잠재 효과)

- 학습 데이터: 지금 파이프라인이 정답을 맞힌 이미지에서 2패스 크롭(날짜 줄)을 저장하고 라벨의 `raw` 를 정답 텍스트로 쓴다. 정답률 71% 라 1,303장 중 약 900장 확보 가능. **1차 500장은 절대 학습에 쓰지 않는다.** 블록 6 이후 + 3차 라벨만.
- PaddleOCR 학습 프레임워크로 `en_PP-OCRv5_mobile_rec` 파인튜닝 (Colab GPU). 사전은 `0-9 A-Z a-z . / - : 공백`. 학습 후 inference 모델로 export → `weights/` 에 교체.
- 제약: paddle 학습 코드가 PaddleOCR 3.x 에서 바뀌었으니 공식 문서의 `tools/train.py` 경로를 확인.

### E. 제출 준비 (Phase 5, 9/14~15, 반드시)

1. Python 3.10 venv 새로 만들어 `pip install -r requirements.txt` → `bash download_weights.sh` → 네트워크 끊고 `jupyter nbconvert --execute predict.ipynb --ExecutePreprocessor.timeout=2400`.
2. `submission.csv` 형식 확인 (image_id, year, month, day, final_date).
3. README 최종 점검, 커밋 해시 제출.
4. 요약서 2p 에 `docs/날짜_해석_규칙.md` 표 삽입.

### 보류

- YOLO 3단계 탐지 (계획서 1.2절). 박스 라벨 0장. B 에서 오탐 비중이 크다고 나오면 Full 탐지만 재검토.
- 합성 데이터 (계획서 2.4절). 유료 VLM API 사용 전 운영진 규정 확인 필요.

---

## 6. 함정 목록 (다시 밟지 말 것)

- **Paddle 이 predict 를 부르면 torch 스레드가 1로 떨어진다.** CRAFT 를 쓰면 4배 느려짐. 지금은 CRAFT 를 안 쓰지만 `ITDA_DET=craft` 경로에 재고정 코드가 있다.
- **torch 를 paddle 보다 먼저 import.** 반대면 Windows 에서 DLL 오류.
- **Windows paddle 3.3.1 탐지 모델은 oneDNN 오류** → `enable_mkldnn=False`. Linux 에서도 같은 설정 유지 (안전).
- **paddleocr 설치가 opencv 를 바꾼다.** requirements 는 opencv-python-headless 와 opencv-contrib-python 을 같은 4.10.0.84 로 고정.
- **pip 설치 중 다른 파이썬 프로세스가 cv2 를 잡고 있으면 실패.** 노트북 돌리면서 설치하지 말 것.
- **Bash heredoc 으로 노트북을 패치하면 백슬래시가 뭉개진다.** 패치 스크립트는 파일로 써서 실행.
- **라벨 CSV 는 엑셀로 열지 않는다.** 앞자리 0 이 사라진다.
- 노트북 결과를 볼 때 `nbconvert --execute` 의 stdout 은 파이프 끝에서 한꺼번에 나온다. 진행률을 보려면 출력 노트북 파일을 열거나 `--output` 파일 시각을 본다.

---

## 7. 팀 상황

- 라벨: 블록 1~7, 9, 10, 14 완료 (1,303장). 블록 8 (민섭), 11~13, 15 남음. 블록 14 에 빈 값 11장 재라벨 필요 (커밋 5d94658).
- 이 PC 의 `.venv` 는 Python 3.13 + torch 2.14 + paddle 3.3.1. 채점 환경(3.10)과 다르므로 최종 검증은 별도 venv.
- 장별 예측·대조 결과는 `results/` 에 있다 (`pred500_*.csv`, `eval500_*.csv`, `pred_rest803_*.csv`, `eval_rest803_*.csv`). eval CSV 의 `err_type` 열로 오답 유형을 바로 필터할 수 있다. 실행된 노트북 파일은 저장하지 않았으니 위 "측정 절차"로 재현.
