# 프로젝트 전체 계획 — [CODE]_서브웨이

> 마지막 갱신: 2026-09-08 (화) · 1차 예선 마감까지 **7일**
> 규정 원문은 [ITDA3_참가안내서.md](ITDA3_참가안내서.md), 운영진 확정 규칙은 [QA_확정사항.md](QA_확정사항.md)

---

## 1. 한 줄 요약

**상품 뒷면 사진 → 소비기한 날짜 하나**를 뽑아내는 프로그램을 만들고, "왜 이렇게 만들었는지"를 2쪽짜리 문서로 설명하는 대회입니다.

---

## 2. 우리가 만드는 것

```
입력: 사진 폴더 (500장, 운영진이 비공개로 준비)
        ↓  predict.ipynb 를 위에서 아래로 실행
출력: submission.csv
      image_id, year, month, day, final_date
      000001,   2027, 06,    26,  2027-06-26
      000002,   NONE, 10,    14,  NONE-10-14     ← 연도가 없는 제품
      000003,   NONE, NONE,  NONE, NONE          ← 날짜가 없는 제품
```

채점은 **연·월·일 각각** 맞으면 점수를 줍니다(부분점수). `NONE`도 정답이 될 수 있습니다.

---

## 3. 점수는 어디서 나오나

| 항목 | 배점 | 우리가 할 일 |
| --- | --- | --- |
| 날짜 추출 정확도 | **50** | 파이프라인 정확도 올리기 |
| CPU 추론 속도 | 10 | 500장을 40분 안에, 가능하면 훨씬 빠르게 |
| 아키텍처 구조도 (요약서 1쪽) | 15 | 파이프라인 그림 + 설명 |
| 설계 논리·최적화·활용 (요약서 2쪽) | **25** | "왜 이 구조인가"를 실측 숫자로 설명 |
| 직접 라벨링 가산점 | +5 | 검증셋 250장 라벨링 (어차피 필요한 일) |

**정성 40점 + 가산점 5점 = 45점이 문서에서 나옵니다.** 정확도만 파면 절반을 버리는 셈이라, 실험할 때마다 "숫자를 남기는" 습관이 중요합니다.

---

## 4. 반드시 지켜야 하는 제약 3개

| 제약 | 뜻 | 어기면 |
| --- | --- | --- |
| **채점 서버는 인터넷 없음** | 모델 가중치를 실행 전에 받아둬야 함 | 노트북 실행 실패 → 정량 0점 |
| **GPU 없는 4코어 CPU** | 무거운 모델은 느림 | 40분 초과 → 속도 0점 (더 넘으면 실행 자체가 죽음) |
| **Python 3.10** | 라이브러리 버전이 3.10에 있어야 함 | pip 설치 실패 → 정량 0점 |

세 개 다 이미 저장소에 반영돼 있습니다(`download_weights.sh`, 속도 최적화, `requirements.txt`). 건드릴 때 깨지지 않게만 조심하면 됩니다.

---

## 5. 파이프라인 구조 (B 플랜: 2-Pass Hybrid)

### 쉽게 말하면

사진에서 글자를 찾는 일(검출)과 읽는 일(인식)은 둘 다 느립니다. 그래서 **찾을 때는 작은 사진으로 대충 찾고, 읽을 때는 날짜로 보이는 부분만 원본에서 자세히 읽습니다.**

### 한 장이 지나가는 길

```
① 사진 열기 + 회전 바로잡기        사진의 8.7%가 옆으로 누워 있음 (EXIF)
② 작게 줄여서 글자 위치 찾기        640px, 장당 1번만 (여기가 제일 비쌈)
③ 큰 글자부터 읽기                  날짜 스탬프는 잔글씨보다 큼 → 찾으면 잔글씨는 건너뜀
④ 날짜 골라내기                     품목보고번호·바코드 같은 긴 숫자 제거 → 날짜 형식 검사
   └ 아무것도 못 찾으면 ②를 1024px로 한 번 더
⑤ 후보 부분만 원본에서 다시 읽기    흐리게 읽힌 숫자 교정
⑥ 여러 개면 가장 늦은 날짜 선택     운영진 확정: 제조일자·소비기한 병기 시 나중 것
```

### 왜 이 구조인가 (요약서 2쪽에 들어갈 핵심)

- 검출 시간은 픽셀 수에 비례: 640px 2.2초 vs 1280px 12초. 날짜 스탬프는 커서 640에서도 잡힘
- 인식 시간은 박스 수에 비례: 박스당 0.17초, 영양성분표가 있으면 50개 → 8.5초. 큰 글자부터 읽고 멈추면 대부분 생략
- 가장 위험한 오탐은 **품목보고번호** (`20130628…`이 날짜처럼 보임) → 9자리 이상 숫자열은 통째로 삭제

### 지금 상태

- 처음부터 끝까지 실행됨, CSV 나옴, 오프라인 로드 확인
- 어려운 케이스 11장 테스트: 정답 아는 10장 중 **완전일치 5 · 부분 1 · 미인식 4**
- 속도: 개발 PC에서 장당 5~7초 (예산 4.8초 — 채점 서버 속도 미확인)

---

## 6. 일정

| 날짜 | 할 일 | 산출물 |
| --- | --- | --- |
| **9/8 (화)** ✅ | 파이프라인 뼈대, 라벨링 도구, Q&A 정리 | `predict.ipynb`, `notebooks/label.py` |
| **9/9 (수)** | 오전: 각자 50장 라벨링 (30분) · 오후: 정확도 측정 + 오류 분석 | `labels/labels_block*.csv`, 오류 분석표 |
| **9/10 (목)** | 오류 분석이 가리키는 곳 튜닝 1차 (저화질·회전·NONE 정책) | 정확도 재측정 |
| **9/11 (금)** | 튜닝 2차 + 속도 최적화 · Colab CPU에서 속도 실측 | 속도 표 |
| **9/12 (토)** | 코드 동결 후보. 요약서 초안 (구조도 + 논리) | 요약서 v1 |
| **9/13 (일)** | **최종 검증**: 3.10 venv에서 clone → 네트워크 끊고 Run All → CSV 확인 | 검증 로그 |
| **9/14 (월)** | 요약서 마무리, 최종 커밋 해시 확정 | PDF, commit hash |
| **9/15 (화)** | 예비일. 23:59 마감 — 오후에 제출 | 이메일 |

9/13을 통으로 검증에 비운 이유: 오프라인 실패·설치 실패는 **정확도와 무관하게 0점**이라 여기서 아끼면 안 됩니다.

---

## 7. 역할 (정해지면 채우기)

| 역할 | 담당 | 내용 |
| --- | --- | --- |
| 파이프라인 | | `predict.ipynb` 튜닝, 속도 |
| 라벨링·EDA | 전원 50장씩 | `labels/`, 오류 분석표 |
| 요약서 | | 구조도 + 설계 논리 2쪽 PDF |
| 검증·제출 | | 3.10 오프라인 실행, 이메일 제출 |

---

## 8. 작업 목록 (우선순위 순)

### 지금 당장
- [ ] 각자 `images.zip` 풀어서 `images/` 만들기
- [ ] 각자 Python 3.10 venv + `pip install -r requirements.txt` + `bash download_weights.sh`
- [ ] 라벨링 블록 담당 정하기 ([labels/README.md](../labels/README.md))

### 9/9
- [ ] 라벨링 250장 완료 → 각자 `labels_block<N>.csv` 커밋
- [ ] 정확도 측정 스크립트 (라벨 vs 예측, 필드별)
- [ ] 오류 분석표: 정확도 × (형식 / 화질 / 회전 / 병기)

### 튜닝 후보 (오류 분석 결과로 순서 결정)
- [ ] 저화질 640×640 군집 (33%) — 업스케일 후 인식 시도
- [ ] 회전 라벨 — 검출 박스가 대부분 세로형이면 90° 돌려 재검출
- [ ] `NONE_POLICY` — 후보 0개일 때 NONE vs 사전확률, 라벨로 비교
- [ ] 인식 박스 상한·조기 종료 비율 조정
- [ ] 키워드(`소비기한`/`까지`) 보조 신호 — 한글 인식기 추가 비용 대비 효과

### 제출 전
- [ ] Colab CPU(2 vCPU)에서 속도 실측 → 500장 예상 시간
- [ ] 3.10 venv, 네트워크 차단, 채점 명령 그대로 Run All
- [ ] 요약서 PDF 2쪽 (`[CODE]_서브웨이_아키텍처구조도.pdf`)
- [ ] `git rev-parse HEAD` → 이메일 본문에 커밋 해시

---

## 9. 리스크와 대응

| 리스크 | 가능성 | 대응 |
| --- | --- | --- |
| 채점 서버가 개발 PC보다 느려 40분 초과 | 중 | Colab CPU 실측. 넘으면 `PASS1_LADDER`를 `(640,)`으로, `MAX_BOXES` 20으로 |
| 저화질 군집에서 정확도 바닥 | 높음 | 구조적 한계. 업스케일 1회 시도 후 안 되면 문서에 한계로 명시 |
| 누군가 `requirements.txt` 를 3.10 미지원 버전으로 바꿈 | 이미 1회 발생 | 파일 상단 주석 + 올릴 때 `pip download --python-version 3.10` 확인 |
| 요약서를 마감 직전에 쓰기 시작 | 높음 | 9/12부터. 구조도는 README 그림 재사용 |
| 라벨링 기준이 사람마다 다름 | 중 | `labels/README.md` 규칙 + `?` 태그 공동 검토 |

---

## 10. 파일 지도

```
predict.ipynb              채점 대상. 첫 셀 CONFIG 수정 금지
requirements.txt           3.10 호환 버전 고정 (건드릴 때 확인 필수)
download_weights.sh        가중치 사전 다운로드
README.md                  실행 가이드 (운영진이 읽음)
docs/
  ITDA3_참가안내서.md        규정 원문
  QA_확정사항.md            운영진 답변 모음 — 설계 근거
  프로젝트_계획.md           이 문서
labels/
  README.md                 라벨링 방법
  sample.csv                라벨링 대상 250장 (블록 배정)
  labels_block<N>.csv       각자 결과 (커밋)
notebooks/
  make_sample.py            층화 샘플링
  label.py                  라벨링 도구
images/, val_images/, weights/, .venv/    gitignore — 각자 로컬
```

---

## 자주 묻는 것

**Q. 코랩 GPU로 학습해도 되나?** 됩니다. 채점만 CPU. 다만 지금 구조는 학습 없이 사전학습 EasyOCR + 규칙이라 GPU가 필요 없습니다.

**Q. `predict.ipynb` 고쳐도 되나?** 첫 셀(CONFIG)만 빼고 자유. 고치면 `val_images/`로 한 번 돌려보고 커밋.

**Q. 파라미터가 어디 있나?** `predict.ipynb` 두 번째 셀. 각 값 위에 실측 근거가 주석으로 있습니다.

**Q. 뭘 물어봐야 할지 모르겠으면?** [QA_확정사항.md](QA_확정사항.md) 맨 아래 "아직 안 물어본 것".
