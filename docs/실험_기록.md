# 실험 기록 — 파이프라인 정확도·속도

> 검증셋: 라벨 653장 (블록 1·2·3·4·6·9 + 블록 5 일부, 2026-09-10 기준). 층화: small 251 · mid 160 · large 242.
> 측정: `scratchpad/eval.py` / `compare.py`. 완전일치 = `final_date` 일치, 필드평균 = 연·월·일 각각 일치율의 평균(채점 방식과 동일).
> 속도: 개발 PC (8 논리코어, EasyOCR CPU). 채점 서버(4 vCPU)에서의 배수는 미확인 — Colab 실측 필요.

## 1. 버전별 결과

| 버전 | 변경 | 완전일치 | 필드평균 | 미인식률 | 답한 것 정밀도 | 커버리지 | 장당 시간 (500장 환산) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 기준선 | 뼈대 (9/8 커밋) | 45.5% | 54.4% | 32.3% | 67.2% | 67.7% | 3.90s (1948s) |
| after | + 연·월 형식, 영문 월 폴백(매 단계), 2자리 연도 상한 | 47.8% | 58.8% | 25.3% | 63.9% | 74.7% | 6.18s (3089s) ✗ |
| **v3** | + 조각 복원, 잡음 숫자 허용, 2패스 확인 전용, 패턴 등급 선택, 조기종료 수정, 폴백 1회 | **53.4%** | **63.8%** | 23.6% | 69.9% | 76.4% | 4.12s (2062s) |
| v4 | + 박스 소프트 상한 30→50 (날짜 비슷한 게 없을 때만) | 53.6% | 63.9% | 23.3% | 69.9% | 76.7% | 4.84s (2421s) ✗ |

**채택: v3.** v4 는 +0.2%p 에 시간 +17% 로 예산 초과. (`MAX_BOXES_HARD = 30` 으로 비활성)

층별 (완전일치 / 필드평균):

| 층 | 기준선 | v3 |
| --- | --- | --- |
| small (≤700px, 33%) | 36.7 / 45.4 | 44.6 / 54.3 |
| mid | 51.2 / 61.5 | 63.7 / 74.2 |
| large (≥2000px) | 50.8 / 59.1 | 55.8 / 66.7 |

형식별 (완전일치, 기준선 → v3): `YYYY.MM.DD` 55.3→62.4 · `YY.MM.DD` 35.1→41.6 · `DD.MM.YYYY` 21.0→30.6 · `MM.YYYY` 0→41.7 · `YYYY.MM` 0→42.9 · 영문 월 0→28.6

## 2. 기준선 오류 분석에서 나온 것 (v3 설계 근거)

| 발견 | 수치 | 대응 |
| --- | --- | --- |
| **미인식이 최대 손실** | 정답 있는 637장 중 211장 NONE | 원인 추적 → 아래 |
| 도트 프린터 날짜가 조각나서 읽힘 | `'20 2 7 . 0 4.13'`, `'2 026 . 0 7 . 3 0'`, `'42025.10.0971741'` | 숫자·구분자 사이 공백 제거본을 함께 파싱 (`squash_digits`), 연도 앞 잡음 숫자 1개 허용 |
| **2패스가 1패스 값을 더 나쁜 값으로 덮음** | 2패스가 덮은 답 완전일치 61.7% vs 1패스 유지 72.3% | 2패스는 교체 금지. 같은 값이면 신뢰도만 올리고(확인), 1패스가 놓친 연도/일만 채움(보완) |
| 공백·6자리 압축 패턴은 잡음 | 선택된 후보 정밀도: `SP_DMY` 12% · `CMP6` 0~13% vs `YYYY` 80% · `DMY4` 78% · `YY` 74% | 패턴 등급(KIND_TIER): 같은 이미지에 1등급 후보가 있으면 2·3등급 무시 |
| "가장 늦은 날짜" 규칙이 미래 연도 잡음에 취약 | `2029-08-14`, `2030-08-28` 류가 정답을 덮음 | 2자리 연도 해석 상한 2027 (라벨 800장 중 2028+ 는 4건, 전부 4자리 인쇄) |
| 조기 종료가 연·월 조각에 걸림 | after 에서 `2026.07.03` → `2026-07-NONE` 4건 퇴행 | 조기 종료는 완전한 1등급 날짜에서만 |
| 영문 월 폴백을 사다리 매 단계 실행 → 속도 악화 | 3.9 → 6.2s/장 | 사다리 마지막 단계에서 1회만 |

## 3. 효과 없었던 것 (기록용 — 요약서 "시도했으나 채택하지 않음")

| 시도 | 결과 | 판단 |
| --- | --- | --- |
| 저화질(640px 원본) 업스케일 후 재검출 | 미인식 10장 기준 1.0배 2/10 · 1.6배 3/10 · 2.0배 2/10, 비용 1.8~2.2배 | 정보 자체가 없음. 채택 안 함. **저화질 군집(33%)이 구조적 천장** |
| 2패스 확인 후보 우선 선택 | 확인 후보 정답률 83.7% vs 미확인 45.1% 로 신호는 강하나, 병기(제조·소비)에서 제조일자를 고름 → 순 −6장 | 채택 안 함 |
| 같은 연·월 후보 중 확인된 것만 유지 / conf 최고만 유지 | 각각 −2, −4, −2장 | 채택 안 함 |
| 2패스 크롭 여백 0.15 → 0.30 | v3 에 포함 (단독 효과 미측정) | 유지 |
| 인식 박스 상한 30 → 50 (날짜 비슷한 게 없을 때만) | +0.2%p, 시간 +17% (4.12 → 4.84s/장) | 채택 안 함 (v4) |

## 4. 남은 오류의 성격 (v3 기준, 653장 중 304장 오답)

- 미인식 ~154장: 대부분 small 층. 인식기가 글자를 못 읽음 (`'4684 23. 0 ../ 615'`). 규칙으로 회수 불가
- 월/일 자리 오독 (연도 맞음): 도트 프린터 `0↔8`, `1↔7`, 자리 탈락. 인식기 품질 문제
- 병기에서 선택 오류: 제조·소비 둘 다 읽혔는데 잘못 고름 — 키워드(`소비기한`/`까지`) 신호가 없어서. 한글 인식기 추가는 비용 대비 검토 필요
- 조각 복원의 부작용: 가짜 후보가 "더 늦은 날짜"로 이김 (12장). 확인 신호로는 못 가림 (§3)

## 4.5 속도 — 채점 서버 흉내 (개발 PC 스레드 제한, 층화 100장)

| 스레드 | 장당 평균 | p90 | 500장 환산 | 판정 |
| --- | --- | --- | --- | --- |
| 8 (개발 PC 기본) | 4.12s (653장) | 7.7s | 2062s | 여유 적음 |
| 4 | 3.37s | 6.2s | 1683s | 안전 |
| 2 | 4.88s | 8.8s | **2438s** | **초과** |

채점 서버 "4 vCPU" 가 물리 2코어+HT 면 2스레드 쪽에 가까움 → 턱걸이 초과 위험. 대응: **시간 예산 가드**(`TIME_BUDGET_S=2000`). 20장 이후 예상 총 소요가 예산을 넘으면 남은 장을 경량 모드로 처리해 완주를 보장한다. 경량 모드에서의 정확도 손실은 사다리(1024 재시도 4.3% 장, 정밀도 50%)와 영문 폴백(3.7%)에 국한.
**가드 발동 검증** (2스레드, 예산을 300s 로 낮춰 강제 발동, 층화 100장): 20장 시점 예상 359s > 300s → 경량 모드 전환 확인. 이후 80장에서 1024 재시도·영문 폴백 0회. 장당 4.96s → 3.66s. 정확도 24/50 → 21/50 (라벨된 50장) — 손실은 사다리·폴백 의존 케이스에 국한. 완주 보장의 대가로 감수.
**Colab CPU 실측 (병철, 2 vCPU Xeon 2.2GHz, Python 3.13, 랜덤 100장): 장당 14.22s, 100장 1422s → 500장 환산 7110s.** 개발 PC 의 3.5배. 채점 서버(4 vCPU)가 Colab 의 2배라 해도 장당 7s = 3500s 로 초과.
→ 가드를 **3단계**로 강화: 1단계(사다리 640만·폴백 생략) → 2단계(640·박스 10·2패스 생략) → 3단계(남은 장 NONE, CSV 는 반드시 생성). 최근 20장 속도로 예측해 단계를 올린다.
- 단계 상승 사슬 검증(예산 100s 강제, 100장): 20장→1단계, 40장→2단계, 60장→3단계, CSV 100행 생성 ✓
- **2단계를 480px 로 설계했던 안은 폐기**: 653장 완전일치 53.4% → **6.4%** (small 0%, mid 0%, large 17%). 640 원본을 480 으로 줄이면 글자가 ~5px 라 인식기가 못 읽음. 해상도는 못 줄인다.
- 정직한 결론: 장당 시간의 절반 이상이 검출기(CRAFT VGG16)이고 이건 EasyOCR 로는 못 줄인다. Colab 급 서버(14s/장)에선 3단계가 불가피 → 처리된 장만 점수. 채점 서버가 AWS c5.xlarge 급(3.4GHz 4vCPU)이면 Colab 의 3~4배라 정상 모드로 완주 가능. **가벼운 전용 검출기(YOLOv8n 급, 50~100ms)가 속도 문제의 근본 해법** — 팀 기술 계획 Phase 2 의 가장 강한 근거.

## 5. 다음 후보 (우선순위)

1. **Colab CPU(2 vCPU)에서 속도 실측** — 개발 PC 4.1s/장이 채점 서버에서 몇 배인지. 2400초 안전선 확인이 최우선
2. 병기 선택: `소비기한`/`까지`/`제조` 키워드를 영문 폴백처럼 **후보가 2개 이상일 때만** 한글 allowlist 로 주변 박스 재인식 → 비용 제한적
3. 인식기 교체 실험 (팀 기술 계획 Phase 1): PaddleOCR rec 을 2패스 확인 단계에만 써서 도트 프린터 오독 개선 여부 확인. 3.10 오프라인 세팅 리스크 큼 — 하루 시간제한
4. 2차 라벨 500장(블록 6~10)으로 파라미터 확정 후 1차 500장에서 최종 보고 수치

## 6. 통합 (`integrate` 브랜치, 9/11) — 서현 PaddleOCR 파이프라인을 기반으로 교체

측정 기준은 이전과 같이 **1차 라벨 500장(블록 1~5, 튜닝에 쓰지 않음)**, 지표는 완전일치 / 필드평균.

| 버전 | 완전일치 | 필드평균 | 장당 시간 (개발 PC 4스레드) | 비고 |
| --- | --- | --- | --- | --- |
| v3 (EasyOCR, main) | 52.8% | 62.9% | 4.1s | 이전 제출본 |
| 서현 PaddleOCR (seohyun-paddleocr, 9/10) | 73.6% | 78.1% | 1.8s | 검증 완료 → 새 기반 |
| + 가드·디버그 CSV 이식 | 73.6% | 78.1% | | 정확도 영향 없음 (가드는 예산 초과 시에만 발동) |
| + 선택 규칙 A+B | **74.2%** | **78.6%** | | 회복 3 / 퇴보 0 |
| + 서현 9/11 최신본 머지 (DOTFIX 코드·파인튜닝 드라이버, DOTFIX 기본 꺼짐) | 74.2% | 78.6% | 2.6s | 회복 0 / 퇴보 0 — 회귀 없음 |

**A+B 내용**: (A) 2자리 연도로 만든 후보 전부에 `YY_FLIP_FROM`(2028) 상한 적용 — `30.07.26` 같은 값이 2030 으로 읽혀 최신 날짜 선택에서 이기던 것 차단. (B) 공백 구분·6자리 압축 패턴(`SP_*`, `CMP6_*`)을 등급 2→3 으로 내려 부분 날짜와 같은 등급으로.

**남은 오답 129장 분해 (디버그 CSV 기준)**: 후보 없음 75 · 후보 전부 오답 31 · 일 오독 17 · 연도만 맞음 5 · 선택 오류 4. 선택 규칙으로 회수할 몫은 4장뿐 → **규칙 튜닝은 여기서 종료, 다음 지렛대는 인식기 파인튜닝**(서현 Phase 3).

**파인튜닝 데이터**: `make_rec_dataset.py --blocks 6,7,9,10,14 --val-blocks 6` → 546 크롭 중 플래그(두 줄·잘림·잡글자) 104 제외 → 학습 393 / 검증 49. `rec_train.zip` 을 Colab `notebooks/finetune_rec_colab.ipynb` 에 올려 학습. 결과 가중치는 500장에서 73.6→? 로 판단 (서현 예상 상한 85~88%).

## 7. 인식기 파인튜닝 (9/11 밤 ~ 9/12, Colab T4 GPU, 병철 실행)

| 버전 | 완전일치 | 필드평균 | 회복 / 퇴보 (기준선 대비) | 장당 시간 |
| --- | --- | --- | --- | --- |
| 기준선 = 서현 폴백 채택본(77.0%) + 선택 규칙 A+B | 77.6% | 83.6% | | 2.98s |
| 1차 파인튜닝 (실제 크롭 393장, 30 epoch, lr 1e-4) | 79.8% | 85.2% | +38 / −27 | 2.66s |
| 2차 파인튜닝 (실제 393 + 합성 도트매트릭스 3,000) | 76.8% | 83.6% | +34 / −38 | 2.26s |
| **1차 파인튜닝 1순위 + NONE 시 사전학습 폴백 (채택)** | **81.0%** | **86.7%** | **+38 / −21** | 2.98s |

- 크롭 단위 검증 정확도(49장)는 사전학습 42.9% → 1차 79.6% → 2차 81.6% 였지만 500장에선 2차가 가장 나빴다. **합성 데이터는 못 읽는 장을 줄이는 대신(NONE 7.8→4.8%) 읽은 값의 정밀도를 떨어뜨렸다(84.2→80.7%).** 49장 검증셋은 채택 근거로 쓰기엔 너무 작다 — 500장이 정본.
- 1차 단독은 이기지만 퇴보 27장이 있고, 그중 21장은 "다른 값"(NONE 아님). 사전학습과 파인튜닝이 서로 다른 장에서 틀리는 점을 이용해 **파인튜닝이 NONE 낸 장(약 7%)만 사전학습 인식기로 재시도**. 폴백이 걸린 장은 500장 중 13장, 시간 증가는 측정 오차 안.
- 시뮬레이션으로 본 다른 조합(사전학습 우선 79.2%, 1차+2차 81.4%, 3자 다수결 81.2%)은 차이가 2장 이내라 잡음. 가중치 하나만 추가로 배포하면 되는 조합을 택했다.
- 구현: `weights/en_PP-OCRv5_mobile_rec_ft/` 가 있으면 1순위 인식기로 로드, `rec_fallback` 에 사전학습 인식기를 들고 있다가 `select_date` 가 None 이면 맞바꿔 1·2패스 재실행. 폴더가 없으면 사전학습만으로 동작(정확도 −3%p, 실패는 아님). env `ITDA_FT_REC=0`, `ITDA_REC_FALLBACK=0` 으로 비교 가능.
- 배포: 가중치 3파일(7.2MB) zip 을 GitHub Release `weights-v1` 첨부 → `download_weights.sh` 가 받아 풀고 `config.json` 은 사전학습 것 복사.
- 학습 재현: `notebooks/finetune_rec_colab.ipynb` (bcebos 휠 직링크, `first_bs`=32 로 T4 OOM 회피). 데이터는 `make_rec_dataset.py --blocks 6,7,9,10,14 --val-blocks 6` 결과에서 플래그(두 줄·잘림·잡글자) 크롭 제외.

### 7.1 서현 밤샘 개선(규칙 v5·v6, 2패스 다수결, 81.8%) 머지 후 재평가 (9/12 오후)

| 버전 (측정용 500장) | 완전일치 | 필드평균 | 회복 / 퇴보 (서현 v6 대비) |
| --- | --- | --- | --- |
| 서현 v6 (사전학습 인식기) | 81.8% | 86.5% | |
| v6 + 선택 규칙 A+B + 파인튜닝 1순위·NONE 시 사전학습 폴백 | 82.8% | 88.0% | +30 / −25 |
| v6 + 선택 규칙 A만 + 같은 폴백 | 82.8% | 88.0% | A+B 와 출력 완전 동일 → **규칙 A·B 는 v6 위에서 효과 0, 제거** |
| **v6 그대로 + 사전학습이 NONE 낸 장만 파인튜닝 인식기로 재시도 (채택)** | **83.2%** | **88.1%** | **+7 / −0** |

- 파인튜닝 인식기를 1순위로 두면 규칙 v6 가 맞히던 25장을 새로 틀린다(전부 "다른 값"). 순서를 뒤집어 **폴백으로만** 쓰면 퇴보가 사라진다. 폴백이 실제로 걸린 장은 500장 중 12장, 장당 시간 +0.2s(2.98→3.19s, 개발 PC 4스레드).
- 결론: integrate = 서현 v6 파이프라인 그대로 + 시간 예산 가드 + 디버그 CSV + 파인튜닝 NONE 폴백. 규칙은 서현 것 하나만 남겨 요약서 규칙표와 코드가 일치한다.

- **미국식 월/일/연 규칙 (9/13)**: 가운데 숫자 13 이상으로 일/월/연이 불가능할 때만 월/일/연 후보(등급 2). 라벨 3,352장 중 확정 미국식 2건뿐이라 기대 이득 ≈0.1%p, 측정 500장 변화 0 (회복 0/퇴보 0). 유럽식과 충돌 없어 채택.
- **제조·포장일자만 있는 사진 (운영진 #15, 9/12)**: 한글 인식기로 3,352장 스캔 → 후보 22 → 육안 확인 7장(0.2%)만 해당. 파이프라인에 한글 키워드 판정을 넣지 않음(기대 이득 500장 중 1~2장, 오판 시 진짜 소비기한을 NONE 으로 만드는 위험이 더 큼). 라벨 7장은 NONE 으로 수정(태그 m).
- **전체 라벨 파인튜닝 (9/13 새벽, Colab T4)**: 3,352장 라벨에서 크롭 추출(블록 6~15, 플래그 제외) → 학습 1,517 / 검증 49, 30 epoch. 검증 크롭 정확도 42.9%(사전학습) → 79.6%(1차 393장) → **89.8%**. 그러나 NONE 폴백으로만 쓰는 500장 파이프라인에선 **83.0% (1차 가중치 83.2% 대비 +2/−3, 잡음)**. 폴백이 걸리는 장이 12장뿐이라 인식기 품질이 결과에 거의 안 실린다. → 1차 가중치(Release weights-v1) 유지, 전체 라벨 가중치는 `코랩_파인튜닝/itda_en_rec_infer_full.zip` 보관.
- **전체 라벨 가중치를 1순위로 (9/13 오후)**: 82.2% (현재 83.2% 대비 +32/−37). 크롭 정확도 89.8% 인 인식기도 1순위로 두면 규칙 v6 가 맞히던 장을 새로 틀린다. → 사전학습 1순위 + 파인튜닝 NONE 폴백 구조 확정.
- **공백 연월 규칙 (`08 2021` → 2021-08-NONE, 9/13 오후)**: 라벨 7건 해당이나 측정 500장 회복 0/퇴보 3 (`22 JUN 2021`, `05 04 2021` 의 조각이 월·연도로 오인). 기각.
- **라벨 검토 (9/13, 병철, 오답 84장 육안)**: r 32 · s 14 · u 12 · p 12 · l 7 · o 6 · f 1. 라벨 오류 7장 수정 → 파이프라인 그대로 **84.2% / 88.8%**. p(병기 오선택) 12장은 정답 날짜가 후보에 없어 규칙 밖 — 조기 종료(EARLY_STOP_RATIO) 완화 실험으로 확인.
- **조기 종료 해제 실험 (EARLY_STOP_RATIO 0.6 → 0, 9/13 오후)**: +2/−1, 시간 +2%. p(병기 오선택) 12장은 작은 글씨를 안 읽어서가 아니라 소비기한 줄 자체를 못 읽은 것. 잡음 수준이라 0.6 유지.
- **9/13 동결 (freeze-0913)**: 측정 500장(라벨 수정 후) **84.2% / 필드평균 88.8%**, 미인식률 3.4%, 개발 PC 4스레드 장당 3.2s. 구성 = 서현 v6 파이프라인 + 시간 예산 가드 + 파인튜닝 인식기 NONE 폴백(Release weights-v1) + 미국식 규칙.

## 8. 본선 (9/22 ~)

측정 기준: 라벨 3,352장 전체 (v6 9/12 실행 `results/pred_all3352_2026-09-12_v6.csv` = 82.0%, 파인튜닝 폴백 없음). 오답 603장 단계별 진단은 `docs/본선계획.md` §2.

| 실험 | 플래그 | 회복 / 퇴보 | 결과 | 판단 |
| --- | --- | --- | --- | --- |
| (k) 2패스는 교정만, 삭제 금지 (`notebooks/exp_p2keep.py`) | `ITDA_P2_KEEP` | 3,352장 +51/−6 (병기 +22/−2 · 비병기 +29/−4) · **판정용 2,852장 +45/−4** | 3,352장 82.0% → 83.4% · **2,852장 81.9% → 83.3%** | **채택 (기본값 켬, 9/23)**. 임계값 없는 구조 규칙, 3분할·봉인 안팎에서 회복이 고름. 퇴보는 1패스 오독이 되살아난 것(003307·002866·001953 은 못 막음, 001116 은 (h) 가 막음). 4스레드 장당 +0.02s |
| (k) + 공백 `YY MM DD` 연도우선 보조 | — | 병기 +23/−4 | | 기각 |
| (h) 연/월/일 범위 가림 · (i) 콜론 구분자 · (j) 한 자리 일 오른쪽 확장 재인식 — 1차 (`notebooks/exp_rules4.py`, `results/exp_rules4_2026-09-23_all4fix_merged.csv`) | `ITDA_RULE_SPAN` / `ITDA_RULE_COLON` / `ITDA_RULE_TRUNC` | 2,852장, (k) 대비 **+23/−14** (병기 +8/−5 · 비병기 +15/−9) | 83.3% → 83.6% | **기준 미달** (퇴보 ≤ 회복/3). 퇴보 14장을 규칙별로 재실행: **전부 (h) 의 "왼쪽 시작 우선" 변형** 탓 — 공백 구분 잡음(`20 2 2020.12.19` 의 `20 2 2020` 이 DMY4)이 먼저 시작해 정답 YYYY 를 지움. (i)(j) 는 퇴보 0. 원래 로직(겹치면 YYYY 우선)으로 되돌림, 000949 1장은 알려진 한계로 둠. 4스레드 장당 +0.27s ((j) 확장 재인식) |
| (h)(i)(j) — 2차, (h) 원래 로직 + (j) `0?` 제거 (`results/exp_all4fix2_2026-09-23_{0,1,2}.csv`) | 위와 같음 | 2,852장, (k) 대비 **+23/−8** (병기 +9/−6 · 비병기 +14/−2) | 83.3% → 83.8% | 회복 23·퇴보 8을 전부 규칙별 재실행으로 귀속: **(h) SPAN +10/−5 (병기 +8/−4) → 기각** — YYYY 매칭이 두 날짜를 가로지르는 유형(`24/10/2021 07/2020` 의 `2021 07/20`)이 5장, 왼쪽 우선으로 뒤집으면 14장 퇴보라 대칭 결함. **(i) COLON +6/−0 → 채택**(전부 연도 NONE → 연도 복구). **(j) TRUNC +6/−0 → 채택**. 나머지 퇴보 3장(001522·001818·002815)은 규칙 탓이 아니라 01:33 main merge 로 들어온 미국식 MDY4(등급 2)가 같은 연·월을 차지해 (k) 가 등급 1 정답을 안 살린 상호작용 |
| **채택 조합 확인: (k)+(i)+(j), SPAN 꺼짐** (`results/exp_kct_2026-09-23_{0,1,2}.csv`, pass1·pass2 1회에 유지 규칙 두 변형) | `ITDA_P2_KEEP`·`ITDA_RULE_COLON`·`ITDA_RULE_TRUNC` 기본 "1" | 기준(v6) 대비 **+56/−5**, (k) 대비 +13/−3 (퇴보 3 = 위 MDY4 상호작용) | **81.9% → 83.7%** (2,335 → 2,386) | 병기 64.1 → 70.5%, 비병기 83.9 → 85.2% |
| **(k) 등급 인식 변형** — 1패스 후보와 패턴 등급이 같거나 높은 2패스 출력만 "같은 연·월을 다시 읽음"으로 인정 | (k) 안에 포함 | 위 조합 대비 **+8/−1** (병기 +3/−1 · 비병기 +5/−0) | **83.7% → 83.9%** (2,393) | **채택 (최종 기본값, 9/23 05:30)**. 회복 8장 중 4장은 연월만(등급 3) → 완전 날짜, 3장은 MDY4 상호작용 해소, 퇴보 1장(001094)은 1패스 잡음. 동기는 3장이었지만 다른 메커니즘 4장이 같이 회복돼 일반화로 판단. 4스레드 60장 속도: 전부 꺼짐 대비 +0.11s (1.94 → 2.04s, p90 2.41s, 500장 +53s) |
| **파인튜닝 인식기 2패스 전용** (`notebooks/exp_ft_p2.py`, 9/11 v1 가중치 `train_out/itda_en_rec/infer`, 크롭 정확도 42.9%) | 코드 없음(실험만) | 최종 규칙 기준선(83.9%, kct_tier 와 2,852장 전부 일치) 대비 — ft_p2 단독 **+51/−65**, ft_union(사전학습 ∪ 파인튜닝 후보) **+33/−23** (병기 +11/−5 · 비병기 +22/−18), ft_fb = ft_p2 | ft_p2 83.4% · ft_union 84.3% | **v1 가중치로는 기각** (둘 다 퇴보 > 회복/3). 병기에서는 union 이 통과권(+11/−5)이라 full 가중치(크롭 89.8%, `weights/en_PP-OCRv5_mobile_rec_ft` 에 아직 없음)로 재실험 가치 있음. 결과 `results/exp_ft_p2_2026-09-23_{0,1,2}.csv` |

결과 파일: `results/exp_p2keep_tag2_2026-09-22.csv`(병기), `results/exp_p2keep_rest_2026-09-22.csv`(비병기), 진단 `results/diag_errors_2026-09-22.csv`, 규칙 4개 1차 `results/exp_rules4_2026-09-23_all4fix_{0,1,2,merged}.csv`, 속도 `notebooks/time_check.py`.

**최종 구성 (9/23 05:30, 커밋 c71659d):** `ITDA_P2_KEEP=1`(등급 인식) · `ITDA_RULE_COLON=1` · `ITDA_RULE_TRUNC=1` · `ITDA_RULE_SPAN=0`. 판정용 2,852장 **81.9% → 83.9%** (v6 대비 회복 63 / 퇴보 5 → 순 +58), 병기 64.1 → 71.1%, 비병기 83.9 → 85.4%. 봉인 500장은 여전히 미측정(9/28 최종 통합 후).

**규칙 채택 규약 (과적합 방지, 9/23 확정):** 규칙은 임계값·크기 상수를 표본에 맞춰 조정하지 않는다(구조 규칙만). 판정은 봉인 500장(블록 1~5)을 뺀 2,852장에서, 회복·퇴보를 **병기·비병기 각각** 보고, 퇴보가 회복의 1/3 을 넘으면 기각. 퇴보 사례는 장별로 재실행해 어느 규칙 책임인지 가른 뒤 그 규칙만 고치거나 버린다. 한 장을 살리려고 규칙을 비틀지 않는다((h) 왼쪽 우선 변형이 그 예: 1장 회복, 14장 퇴보).

