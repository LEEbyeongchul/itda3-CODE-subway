# 실행 환경: Python 3.10 (운영진 채점 서버 기준). 3.11 까지는 동일 버전으로 동작 확인.
# 3.12+ 에서는 torch 2.2.2 wheel 이 없으므로 반드시 3.10 venv 를 쓰세요:  py -3.10 -m venv .venv
# 버전을 올릴 때는 반드시 `pip download --python-version 3.10 --only-binary=:all: <pkg>==<ver>` 로 3.10 wheel 존재를 확인할 것.
# macOS 사용자는 torch/torchvision 의 '+cpu' 접미사를 제거하고 설치하세요 (맥에는 +cpu 휠이 없습니다).
--extra-index-url https://download.pytorch.org/whl/cpu

# ── 예시 스택입니다. 팀에서 사용하는 라이브러리로 자유롭게 교체하되,
#    버전은 반드시 == 로 고정해 주세요. ──
torch==2.2.2+cpu
torchvision==0.17.2+cpu
easyocr==1.7.1                    # 탐지기 CRAFT (인식기는 아래 PaddleOCR)
paddlepaddle==3.3.1               # CPU 빌드. torch 를 먼저 import 해야 함 (predict.ipynb 가 그렇게 함)
paddleocr==3.7.0                  # PP-OCRv5 영문 인식기. 3.10 휠 확인: win_amd64 / manylinux 모두 OK (2026-09-10)
opencv-python-headless==4.10.0.84 # easyocr 의존
opencv-contrib-python==4.10.0.84  # paddlex 의존. 같은 버전으로 맞춰 cv2 네임스페이스 충돌 방지
pandas==2.2.2
numpy==1.26.4
Pillow==10.3.0

# ── 아래 2개는 채점 노트북 실행에 필요합니다. 삭제하지 마세요. ──
nbconvert==7.16.4
ipykernel==6.29.4
