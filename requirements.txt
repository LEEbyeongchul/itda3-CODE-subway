# 실행 환경: Python 3.13 (Windows/Linux CPU)
# macOS 사용자는 torch/torchvision 의 '+cpu' 접미사를 제거하고 설치하세요 (맥에는 +cpu 휠이 없습니다).
--extra-index-url https://download.pytorch.org/whl/cpu

# ── 추론 스택 ──
torch==2.14.0+cpu
torchvision==0.29.0+cpu
easyocr==1.7.2
opencv-python-headless==5.0.0.93
pandas==3.0.5
numpy==2.5.3
Pillow==12.3.0

# ── 아래 2개는 채점 노트북 실행에 필요합니다. 삭제하지 마세요. ──
nbconvert==7.17.1
ipykernel==7.3.0
